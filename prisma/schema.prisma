generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("POSTGRES_PRISMA_URL")
}

model Video {
  id          String     @id @default(uuid())
  title       String     @unique
  description String
  url         String
  categories  Category[]

  @@map("Videos")
}

model Category {
  id     String  @id @default(uuid())
  title  String  @unique
  color  String
  videos Video[]

  @@map("Categories")
}
